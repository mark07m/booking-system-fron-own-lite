# ===========================================
# BOOKING SYSTEM - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env.local and fill in your values

# ===========================================
# PUBLIC VARIABLES (Available on client-side)
# ===========================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api
NEXT_PUBLIC_APP_NAME=Booking System
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_ENVIRONMENT=development

# ===========================================
# SERVER-ONLY VARIABLES (Not available on client-side)
# ===========================================

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/booking_system

# Authentication Configuration
AUTH_COOKIE_NAME=booking_auth
AUTH_SECRET=your-32-character-secret-key-here-change-this-in-production
AUTH_URL=http://localhost:3000

# ===========================================
# EXTERNAL SERVICES (Optional)
# ===========================================

# Stripe Payment Processing
STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key_here
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Redis Cache (Optional)
REDIS_URL=redis://localhost:6379

# ===========================================
# FILE STORAGE (Optional)
# ===========================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# ===========================================
# RATE LIMITING (Optional)
# ===========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# DEVELOPMENT NOTES
# ===========================================
# 1. Make sure to change AUTH_SECRET to a secure random string
# 2. Update database credentials for your environment
# 3. Configure email settings if you need email notifications
# 4. Add Stripe keys if you need payment processing
# 5. Set REDIS_URL if you want to use Redis for caching
